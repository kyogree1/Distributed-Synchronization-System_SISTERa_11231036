# Laporan: Implementasi Distributed Synchronization System
Nama / NIM: (isi)
Mata Kuliah: Sistem Paralel dan Terdistribusi

## 1. Arsitektur
- Diagram + penjelasan komponen (lihat `docs/architecture.md`).

## 2. Algoritma
- Raft (pemilihan leader, heartbeats, replikasi state machine).
- Queue (consistent hashing, at-least-once, recovery).
- Cache (MESI-lite, invalidation, LRU).

## 3. API (Swagger)
- Lihat `docs/api_spec.yaml` atau `/docs` pada tiap node.

## 4. Deployment
- Langkah build & run (Docker Compose).

## 5. Pengujian & Performa
- Skenario benchmark yang dijalankan (latency, throughput).

## 6. Video
- Link YouTube publik.

## 7. Tantangan & Pekerjaan Lanjutan
- Deadlock detection, full Raft log matching, dynamic membership, security, PBFT (bonus).
